# 测试计划 (Test Plan)

> **占位模板,本版未使用**。设计文档 §2.1、§5.6 明确:测试 agent 工具画像(Read/Grep/Write 测试文件/Bash 跑测试)已就位,但**接口测试 / Playwright 功能测试本版 defer**,MVP 只行使需求符合性走查。
>
> 此文件保留结构,供后续测试执行能力落地时使用。

## 1. 走查范围(MVP,当前生效)
<!-- 由测试 agent 在交接块 review-findings.spec 中产出 -->

## 2. 接口测试(defer)
<!-- 后续:基于 design-doc §4 接口清单生成接口用例 -->

## 3. Playwright 功能测试(defer)
<!-- 后续:基于 R-id 验收标准生成 E2E 用例 -->

## 4. 三级门禁语义(为后续预留)
- ① 走查不过 → 中止(代码连需求都没满足,白跑昂贵 E2E 是浪费)。
- ① 过 → ② 接口测试 + ③ E2E 互补都跑。
- 本版不实现。
