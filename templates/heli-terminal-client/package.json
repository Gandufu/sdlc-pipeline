{
  "name": "heli-terminal-client",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "corepack pnpm -r --parallel --filter ./packages/main --filter ./packages/renderer run dev",
    "build": "corepack pnpm -r run build",
    "test": "corepack pnpm --filter @heli/shared build && corepack pnpm -r run test",
    "typecheck": "corepack pnpm -r run typecheck"
  },
  "devDependencies": {
    "typescript": "5.4.5",
    "@types/node": "20.12.7"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20.11.0",
    "pnpm": ">=9.0.0"
  }
}
